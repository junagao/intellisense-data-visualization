import styled from 'styled-components';

const AxisG = styled.g`
  path,
  line {
    stroke: black;
    opacity: 0.2;
    stroke-width: 0.5;
    shape-rendering: crispEdges;
  }
  text {
    opacity: 0.75;
  }
`;

export default AxisG;
